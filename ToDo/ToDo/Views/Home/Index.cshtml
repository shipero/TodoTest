@model IEnumerable<ToDo.Models.ToDoModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.UserProfileUserId)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.ToDoDisplay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Completed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.UserProfileUserId)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.ToDoDisplay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Completed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ToDoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ToDoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ToDoId })
        </td>
    </tr>
}

</table>
